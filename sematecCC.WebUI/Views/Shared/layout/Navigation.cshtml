@using Core.Domain.Entities
@{
    var list = ViewBag.Menus == null ? new List<Menu>() : ViewBag.Menus as List<Menu>;
}


<div id="sidebar-menu">
    <ul>
        <li class="text-muted menu-title">پنل برنامه</li>
        @foreach (Menu menu in list)
        {
            if (menu.Children.Count == 0)
            {
                <li><a href="~/" class="waves-effect"><i class="fa fa-tachometer" aria-hidden="true"></i> <span> @menu.Label </span> </a></li>

            }
            
            else
            {
                <li class="has_sub" parent-menu="@menu.Name">
                    <a class="waves-effect">
                        <i class="fa fa-cogs" aria-hidden="true"></i>
                        <span>@menu.Label</span>
                        <span class="menu-arrow"></span>
                    </a>

                    @* ✅ یک بار <ul> را ایجاد کنید *@
                    <ul class="list-unstyled">
                        @foreach (Menu submenu in menu.Children)
                        {
                            <li selected-menu="@submenu.Name">
                                <a href="@Url.Action(submenu.Action, submenu.Controller, new { area = "" })">
                                    @submenu.Label
                                </a>
                            </li>
                        }
                    </ul>
                </li>
            }
           
        }




    </ul>
    <div class="clearfix"></div>
</div>